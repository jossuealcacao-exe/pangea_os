# Pangea OS

Read `AGENTS.md` as the universal entry, then load only the files needed for the active task.

Project truth lives in `/agent`. Use `.claude/skills/pangea` for orchestration and `.claude/skills/agent` for AHP+.

Do not preload every site or every engine. Resolve the active project first. Never claim tests or changes without tool evidence. Do not push, deploy, read secrets or make irreversible changes without explicit approval.
