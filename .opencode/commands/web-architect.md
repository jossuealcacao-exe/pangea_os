---
description: Plan, build, audit or compile a web project with Web Architect OS
agent: web-build
---

Load the `web-architect` skill using the skill tool.

Treat the following text as the project request:

$ARGUMENTS

If no mode is explicit, use BUILD when a local repository is available and PROMPT_ONLY when the user asks only for a prompt. Inspect before editing, activate only relevant engines, continue through QA and report verified results.
