---
description: Run the Web Architect OS quality gate
agent: web-build
---

Load the `web-qa` skill using the skill tool.

Scope or emphasis:

$ARGUMENTS

Run only tests supported by the actual repository. Separate executed evidence from inference, fix only inside authorized scope and produce a QA report.
